<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="org.idp.sample.infrastructure.datasource.tenant.TenantMapper">

    <insert id="insert">
        INSERT INTO tenant(id, name, type, issuer)
        VALUES
            (
              #{tenant.identifier.value},
             #{tenant.name.value},
             #{tenant.type},
             #{tenant.issuer}
            );
    </insert>

    <resultMap id="Tenant" type="org.idp.sample.domain.model.tenant.Tenant">
        <id property="identifier.value" column="id"/>
        <result property="name.value" column="name"/>
        <result property="type" column="type"/>
        <result property="issuer" column="issuer"/>
    </resultMap>

    <sql id="select">
        SELECT
            id,
            name,
            type,
            issuer
        FROM tenant
    </sql>

    <select id="selectBy" resultMap="Tenant">
        <include refid="select" />
        WHERE
         id = #{tenantIdentifier.value}
    </select>


</mapper>