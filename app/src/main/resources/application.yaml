idp:
  configurations:
   basePath: "src/main/resources/config/sample"
   publicTenantDomain: "${PUBLIC_TENANT_DOMAIN:http://localhost:8080/}"


spring:
  datasource:
    url: "jdbc:postgresql://localhost:5432/idpserver"
    username: "idpserver"
    password: "idpserver"
    hikari:
      connection-timeout: 3000
      maximum-pool-size: 50
      minimum-idle: 50
logging:
  level:
    root: info
    web: info
    org.mybatis: debug
    org.idp.sample: debug

