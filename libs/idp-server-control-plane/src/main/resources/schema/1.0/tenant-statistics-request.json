{
  "type": "object",
  "properties": {
    "from": {
      "type": "string",
      "pattern": "^\\d{4}-\\d{2}$",
      "description": "Start month for statistics (YYYY-MM format, e.g., 2025-01)"
    },
    "to": {
      "type": "string",
      "pattern": "^\\d{4}-\\d{2}$",
      "description": "End month for statistics (YYYY-MM format, e.g., 2025-12)"
    },
    "limit": {
      "type": "string",
      "pattern": "^([1-9][0-9]?|100)$",
      "description": "Maximum number of records to return (1-100)"
    },
    "offset": {
      "type": "string",
      "pattern": "^(0|[1-9][0-9]*)$",
      "description": "Number of records to skip for pagination (>=0)"
    }
  },
  "required": ["from", "to"],
  "additionalProperties": true
}
